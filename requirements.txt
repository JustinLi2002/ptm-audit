numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
torch>=2.0          # training only
fair-esm>=2.0       # language model embeddings only
networkx            # interaction embeddings only
node2vec            # interaction embeddings only
gensim              # interaction embeddings only
